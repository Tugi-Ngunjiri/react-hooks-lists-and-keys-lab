import React from "react";

function NavBar() {
  const links = [ "home" , "about" ,"projects"];

  return <nav>{/*display an <a> tag for each link here */}</nav>;
};

<a href="links"> home,about,projects</a>;


export default NavBar;